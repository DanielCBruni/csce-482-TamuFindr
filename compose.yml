services:
  web-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: tamufindr-dev
    container_name: tamufindr-dev-v1
    # environment:
    #   NODE_ENV: production
    #   PORT: "3000"
    ports:
      - "80:3000"
    restart: unless-stopped